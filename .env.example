# Đổi tên file này thành .env và cập nhật giá trị thực tế
VITE_APP_NAME=VNR
VITE_API_BASE_URL=http://localhost:8080/api

# OpenAI (chỉ dùng trên server – KHÔNG thêm prefix VITE_)
OPENAI_API_KEY=sk-your-openai-api-key
# Model được phép: gpt-4o (mặc định) hoặc gpt-5-mini
OPENAI_MODEL=gpt-4o
